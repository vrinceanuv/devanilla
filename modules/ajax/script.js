/**
 * Does an ajax call
 * @param  {string} url - The url for the ajax call
 * @param  {string} type - The type of the call: get, post, put, delete
 * @param  {function} callback - The callback after the call is done
 * @param  {string} data - The data you want to send when doing the call
 */
export const ajaxCall = (url, type, callback, data) => {
  // TODO ajax call
  return data;
};
